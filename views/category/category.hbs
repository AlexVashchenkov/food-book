<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Блюда категории {{category.name}}</title>
  {{> styles}}
  <style>
    .dishes-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 20px;
    }
    .page-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 30px;
      padding-bottom: 15px;
      border-bottom: 1px solid #eee;
    }
    .dishes-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 25px;
    }
    .dish-card {
      background: white;
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 3px 10px rgba(0,0,0,0.1);
      transition: all 0.3s ease;
    }
    .dish-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 5px 15px rgba(0,0,0,0.2);
    }
    .dish-image {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }
    .dish-info {
      padding: 15px;
    }
    .dish-name {
      font-size: 1.2rem;
      margin: 0 0 10px 0;
      color: #333;
    }
    .dish-description {
      color: #666;
      font-size: 0.9rem;
      margin-bottom: 15px;
    }
    .dish-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .dish-price {
      font-weight: bold;
      color: #2e7d32;
      font-size: 1.1rem;
    }
    .dish-actions {
      display: flex;
      gap: 10px;
    }
    .btn {
      padding: 5px 12px;
      border-radius: 4px;
      text-decoration: none;
      font-size: 0.9rem;
      cursor: pointer;
    }
    .btn-edit {
      background-color: #2196F3;
      color: white;
      border: none;
    }
    .btn-delete {
      background-color: #f44336;
      color: white;
      border: none;
    }
    .add-dish-btn {
      background-color: #4CAF50;
      color: white;
      padding: 8px 16px;
      border-radius: 4px;
      text-decoration: none;
    }
    .back-btn {
      color: #2196F3;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      margin-bottom: 20px;
    }
    .no-dishes {
      text-align: center;
      padding: 40px;
      background: #f9f9f9;
      border-radius: 8px;
    }
  </style>
</head>
<body>
{{> header}}

<main class="dishes-container">
  <a href="/categories" class="back-btn">← Вернуться к категориям</a>

  <div class="page-header">
    <h1>Блюда категории: <span style="color: {{category.color}}">{{category.name}}</span></h1>
    <a href="/categories/{{category.id}}/dishes/add" class="add-dish-btn">+ Добавить блюдо</a>
  </div>

  {{#if dishes.length}}
    <div class="dishes-grid">
      {{#each dishes}}
        <div class="dish-card">
          {{#if this.image}}
            <img src="{{this.image}}" alt="{{this.name}}" class="dish-image">
          {{else}}
            <div class="dish-image" style="background: #f0f0f0; display: flex; align-items: center; justify-content: center;">
              <span>Нет изображения</span>
            </div>
          {{/if}}
          <div class="dish-info">
            <h3 class="dish-name">{{this.name}}</h3>
            <p class="dish-description">{{this.description}}</p>
            <div class="dish-footer">
              <span class="dish-price">{{this.price}} ₽</span>
              <div class="dish-actions">
                <a href="/categories/{{../category.id}}/dishes/{{this.id}}/edit" class="btn btn-edit">Изменить</a>
                <button onclick="deleteDish({{../category.id}}, {{this.id}})" class="btn btn-delete">Удалить</button>
              </div>
            </div>
          </div>
        </div>
      {{/each}}
    </div>
  {{else}}
    <div class="no-dishes">
      <p>В этой категории пока нет блюд.</p>
      <a href="/categories/{{category.id}}/dishes/add" class="add-dish-btn">Добавить первое блюдо</a>
    </div>
  {{/if}}
</main>

{{> footer}}
<script>
  async function deleteDish(categoryId, dishId) {
    if (!confirm('Вы уверены, что хотите удалить это блюдо?')) return;

    try {
      const response = await fetch(`/categories/${categoryId}/dishes/${dishId}`, {
        method: 'DELETE'
      });

      if (response.ok) {
        window.location.href = '/categories'
      } else {
        const error = await response.json();
        alert(error.message || 'Ошибка при удалении');
      }
    } catch (error) {
      console.error('Error:', error);
      alert('Ошибка при удалении блюда');
    }
  }
</script>
</body>
</html>
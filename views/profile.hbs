<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
  {{> styles}}
</head>
<body>
{{> header}}
<main class="profile-main">
  <div class="profile-container">
    <div class="profile-card">
      <img src="{{user.image}}" alt="Фото пользователя" class="profile-photo">
      <div class="profile-info">
        <h2>{{user.name}}</h2>
        <p><strong>Возраст:</strong> {{user.age}} лет</p>
      </div>
    </div>
    <div class="profile-actions">
      <a href="/add-new-meal" class="btn add-meal-btn">Добавить новое блюдо</a>
      <form action="/login" method="GET" class="logout-form">
        <button type="submit" class="btn logout-btn">Выйти</button>
      </form>
    </div>
  </div>
</main>
{{> footer}}
</body>
</html>